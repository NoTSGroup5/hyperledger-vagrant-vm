# Hyperledger Virtual Machine

A virtual machine made with Vagrant and provisioned with Ansible

## Prerequisites
* Virtualbox
* Vagrant
* Ansible
* NFS Server

## Install NFS
```
apt-get install nfs-kernel-server
```
```
apt-get install nfs-common
```

## How to run

`vagrant up`

